#include<stdio.h>
void  select(char,int);
void date();
void seats(int a,char x){
	int i,j,s=a;
	char z=x;
	printf("how many seats do u want to book\n");
	printf("press 0 for previous menu\n");
	scanf("%d",&i);
	printf("char=%c int=%d",z,s);
	if(i==0)
	date();
	else
	 select(z,s);
}

